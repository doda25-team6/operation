# Standard localhost entries (needed for self-naming)
127.0.0.1       localhost
::1             localhost ip6-localhost ip6-loopback

# List of all cluster nodes generated by Ansible
# IP addresses passed from Vagrant via extra_vars

{{ ctrl_ip }} ctrl
{% for i in range(worker_ips|length) %}
{{ worker_ips[i] }} node-{{ i + 1 }}
{% endfor %}
