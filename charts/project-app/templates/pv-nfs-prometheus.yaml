{{- if .Values.prometheus.persistence.enabled }}
apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ include "project-app.fullname" . }}-prometheus-pv
  labels:
    app: {{ include "project-app.fullname" . }}
spec:
  capacity:
    storage: {{ .Values.prometheus.persistence.size }}
  accessModes:
    - {{ .Values.prometheus.accessMode }}
  storageClassName: nfs-prometheus
  persistentVolumeReclaimPolicy: Retain
  nfs:
    server: {{ .Values.persistence.nfs.server }}
    path: {{ .Values.persistence.nfs.path }}
{{- end }}
