apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "project-app.fullname" . }}-v1
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "project-app.fullname" . }}
      version: v1
  template:
    metadata:
      labels:
        app: {{ include "project-app.fullname" . }}
        version: v1
    spec:
      containers:
      - name: project-app
        image: "{{ .Values.image.repository }}:{{ .Values.appVersions.v1.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        ports:
          - containerPort: {{ .Values.service.port }}
        env:
          - name: MODEL_HOST
            valueFrom:
              configMapKeyRef:
                name: {{ include "project-app.fullname" . }}-config
                key: model-host
          - name: LOG_LEVEL
            value: "{{ .Values.config.logLevel }}"
          - name: EXAMPLE_VALUE
            value: "{{ .Values.config.exampleValue }}"
          - name: EXAMPLE_SECRET
            valueFrom:
              secretKeyRef:
                name: {{ include "project-app.fullname" . }}
                key: example-secret

        volumeMounts:
        {{- if .Values.persistence.enabled }}
        - name: shared-storage
          mountPath: {{ .Values.persistence.mountPath }}
        {{- end }}

      volumes:
      {{- if .Values.persistence.enabled }}
      - name: shared-storage
        persistentVolumeClaim:
          claimName: "{{ if .Values.persistence.claimName }}{{ .Values.persistence.claimName }}{{ else }}{{ include "project-app.fullname" . }}-pvc{{ end }}"
      {{- end }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "project-app.fullname" . }}-v2
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "project-app.fullname" . }}
      version: v2
  template:
    metadata:
      labels:
        app: {{ include "project-app.fullname" . }}
        version: v2
    spec:
      containers:
      - name: project-app
        image: "{{ .Values.image.repository }}:{{ .Values.appVersions.v2.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        ports:
          - containerPort: {{ .Values.service.port }}
        env:
          - name: MODEL_HOST
            valueFrom:
              configMapKeyRef:
                name: {{ include "project-app.fullname" . }}-config
                key: model-host
          - name: LOG_LEVEL
            value: "{{ .Values.config.logLevel }}"
          - name: EXAMPLE_VALUE
            value: "{{ .Values.config.exampleValue }}"
          - name: EXAMPLE_SECRET
            valueFrom:
              secretKeyRef:
                name: {{ include "project-app.fullname" . }}
                key: example-secret

        volumeMounts:
        {{- if .Values.persistence.enabled }}
        - name: shared-storage
          mountPath: {{ .Values.persistence.mountPath }}
        {{- end }}

      volumes:
      {{- if .Values.persistence.enabled }}
      - name: shared-storage
        persistentVolumeClaim:
          claimName: "{{ if .Values.persistence.claimName }}{{ .Values.persistence.claimName }}{{ else }}{{ include "project-app.fullname" . }}-pvc{{ end }}"
      {{- end }}
